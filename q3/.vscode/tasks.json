{
    "tasks": [
      {
        "type": "shell",
        "label": "yacc and lex",
        "command": "lex ${fileBasenameNoExtension}.l;gcc lex.yy.c -ll -w;./a.out",
        "options": {
          "cwd": "${fileDirname}"
        },
        "problemMatcher": [],
        "group": {
          "kind": "build",
          "isDefault": true
        }
      }
    ],
    "version": "2.0.0"
  }